// Definimos un array con cinco colores
let colores = ["rojo", "verde", "azul", "amarillo", "naranja"];

// Pedimos al usuario un color
let nuevoColor = prompt("Introduce un color que te gustaría añadir:");

// Añadimos el nuevo color al final del array (EXTRA)
colores.push(nuevoColor);

// Mostramos el array actualizado
console.log("Array actualizado de colores:", colores);
